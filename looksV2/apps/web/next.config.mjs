/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // If the site is deployed to a subpath (e.g. /repository-name), uncomment and set basepath:
  // basePath: '/lakhnavi-sewa-in',
};

export default nextConfig;
